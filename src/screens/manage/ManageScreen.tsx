import React from 'react';
import {SafeAreaView, View, Text} from 'react-native';

const ManageScreen = () => {
  return (
    <SafeAreaView
      style={{
        flex: 1,
      }}>
      <View
        style={{
          flex: 1,
          marginTop: 24,
        }}>
        <View
          style={{
            flex: 1,
            flexDirection: 'row',
          }}>
          <Text>dsd</Text>
        </View>
      </View>
    </SafeAreaView>
  );
};

export default ManageScreen;
